<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.kh.plklj.delivery.mapper.DeliveryMapper">

	<!-- 배송 리스트 조회-->
	<select id="selectDeliveryList" resultType="Manage">
		SELECT DELIVERY_NO, PIECE_NO, MEMBER_NO, DELIVERY_DATE, PIECE_TITLE, FINAL_PRICE, SELL_PRICE,  DELIVERY_STATUS
		FROM DELIVERY
		JOIN PIECE USING(MEMBER_NO)
		LEFT JOIN AUCTION USING(PIECE_NO)
		LEFT JOIN PIECE_SELL USING(PIECE_NO)
		WHERE MEMBER_NO = {memberNo}
		ORDER BY DELIVERY_DATE DESC
	</select>

</mapper>